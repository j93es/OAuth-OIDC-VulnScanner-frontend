[package]
name = "is-html-fast"
version = "0.1.0"
edition = "2024"

[dependencies]
futures = "0.3.31"
reqwest = { version = "0.12.19", features = ["blocking", "json"]}
tokio = { version = "1", features = ["full"] }
